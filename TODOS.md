# TODOS — Marketplace MVP

Generated by `/plan-ceo-review` on 2026-05-21 (SCOPE EXPANSION mode).
Single source of truth for everything deferred or pending. Keep dates as-of when added.

---

## P1 — Before First Real Seller (accepted 2026-05-21)

Critical gaps that surfaced in the review and were greenlit. All four shipped 2026-05-21.

- [x] **P1.1 — OTP brute-force lockout** — `src/lib/auth.ts` consumes the OTP code after 5 failed attempts (forces attacker to wait for a fresh code request, which won't come unless they're the real user). Simpler than a separate `lockedUntil` field; same brute-force resistance.
- [x] **P1.2 — Rate limiting** — `src/lib/ratelimit.ts` with ioredis sliding-window + in-process fallback. Wraps `/api/otp/send` (5/min/IP, 3/min/phone), `/api/cart` POST (30/min/user), `/api/reviews` POST (5/min/user), `/api/checkout` POST (5/min/user). 429 with Retry-After header.
- [x] **P1.3 — Webhook signature verification** — `/api/webhooks/stripe` uses `stripe.webhooks.constructEvent` against raw body; `/api/webhooks/razorpay` does constant-time HMAC-SHA256 verify. Both keep stub-mode for dev when secrets unset. Both reject `NO_SIGNATURE` / `BAD_SIGNATURE` with 401.
- [x] **P1.4 — Refund dead-letter queue** — New `payments.refund` BullMQ queue with exponential backoff (5 attempts). `disputes.resolveDispute` now calls `enqueueRefund` instead of `refundOrder`. Worker (`src/workers/ai.ts:startRefunds`) handles retries. Stuck refunds surface as `Order.status=REFUNDED` + `Payment.status=CAPTURED` mismatch (queryable signal for ops).

## P1 — Foundation Hardening (accepted 2026-05-21)

- [ ] **Baseline test coverage** (~3 days CC, see [D11]) — Vitest unit tests for pure-function libs first (`fees`, `format`, `payouts`, `badges`, `fraud`). Playwright E2E for the seven module happy paths. CI gate on green.
- [ ] **Sentry instrumentation** (~30 min CC, see [D12]) — `@sentry/nextjs` for web, `@sentry/node` for worker. Wire DSN to env. Source maps uploaded in build.
- [ ] **Production deploy target** (see [D13]) — Vercel (web) + Fly.io (worker) + Neon (Postgres) + Upstash (Redis). Wire CI deploy on tag.
- [ ] **Migrate `db:push` → `db:migrate`** (~15 min CC) — generate the initial migration; switch dev workflow; document in README.
- [ ] **PgBouncer / Prisma connection pooling** — set up before first 50 concurrent users. Neon includes this by default; verify.

## P0 — Accepted Cathedral Expansions (CEO plan)

Five expansions accepted in D2-D8. Full detail in `~/.gstack/projects/amazon/ceo-plans/2026-05-21-marketplace-cathedral-expansion.md`. Implementation order:

1. [x] **D8 — Smart Pricing** — shipped 2026-05-21. `src/lib/pricing.ts` (median/p25/p75 from real comparables, last 90 days, Claude polish when ANTHROPIC_API_KEY set, category benchmark fallback). `GET /api/products/[id]/pricing-suggestion`. `PriceSuggestion` panel in `DraftEditor` with "Use $X" button + position badge (below/in-range/above market). Worker `startPricing` upgraded to use the same engine so initial AI price is market-aware. SoldComparable table deferred to P3 (uses live Product rows as comparables for now, which is sufficient until volume).
2. [x] **D4 — Customs Pre-Clearance** — shipped 2026-05-21. `src/lib/customs.ts` (HS codes + US MFN duty by category: handicrafts 3.5%, textiles 6.5%, jewelry 5.5%, default 5%; non-US destinations get 0 duty). `estimateLanded()` computes subtotal + shipping + duty + service. `src/lib/orders.ts:createOrderFromCart` uses landed total. Checkout summary now shows itemized breakdown with category × rate disclosure (e.g. "US import duty (textiles 6.5%)"). Service fee: 4% with $1.99 floor (new `buyerServiceFeeUsdCents` in fees.ts). DDP messaging: "Duty + shipping are prepaid — no surprise charges at delivery." Schema unchanged: duty + service folded into existing `Order.feesUsdCents` with a TODO(schema) for breaking them out into their own columns later.
3. [ ] **D2 — AI Shopping Concierge** (~45 min CC) — new `src/lib/concierge.ts`. Replaces `catalog.search` and `/search/page.tsx`. Streaming results with rationale per item. Mobile voice input.
4. [ ] **D6 — Trust Engine** (~90 min CC) — `src/lib/trust-score.ts` replaces `badges.ts`. New `BuyerProtection` model + `Shop.trustScore`. Dynamic 0-100 score.
5. [ ] **D3 — Provenance Stories** (~3-4 hrs CC) — new `ai.story_video` queue + Remotion render + ElevenLabs TTS. New `Shop.storyVideoUrl`.

## P1 Schema Changes Required (for accepted expansions)

- [ ] `Shop.storyVideoUrl` String? — for D3
- [ ] `Shop.trustScore` Int @default(50) — for D6
- [ ] New `BuyerProtection` model — for D6
- [ ] New `SoldComparable` model — for D8
- [ ] `AiJobKind` enum: add `STORY_VIDEO` — for D3
- [ ] `OtpCode.lockedUntil` DateTime? — for P1.1

## P2 — Security / Hardening Backlog (from review)

- [ ] CAPTCHA on signup (hCaptcha or Turnstile)
- [ ] Zod schema for `Order.shippingAddress` JSON field (validate at action boundary)
- [ ] Prisma log level to `["error"]` in prod + PII redaction middleware
- [ ] Input length caps on `Dispute.description` (max 5000), `Review.body` (max 2000)
- [ ] Signin error messages unified to "Invalid credentials" (no phone-vs-code distinction → blocks enumeration)
- [ ] Server-action wrapper helper that enforces `requireRole` before body runs

## P2 — Performance Backlog (from review)

- [ ] `catalog.listProducts` add `include: { images: { take: 1, orderBy: position } }` (kill N+1)
- [ ] Redis cache for FX rate (1 hr TTL), shop info (5 min TTL), catalog top-level reads (1 min TTL)
- [ ] DB index `Review @@index([productId, createdAt])`
- [ ] Postgres FTS index on Product (or skip — D2 replaces the search)

## P2 — Observability Follow-Ups (D12 deferred)

- [ ] Pino structured logging with request ID middleware
- [ ] Bull Board UI for worker queue visibility (`bull-board`)
- [ ] Per-action metrics (Vercel KV counters or PostHog events)
- [ ] OpenTelemetry tracing across web → worker boundary
- [ ] Alert rules on Sentry (error rate, p99 latency, worker queue depth)

## P2 — Edge Cases (from Section 4)

- [ ] Inventory race: two buyers add last unit to cart, both pay. Add `Product.inventory` decrement inside Order $transaction with stock-check guard.
- [ ] AI pipeline rollback: if categorization fails after description+pricing succeed, product is stuck DRAFT with no rollback path. Add pipeline-status orchestrator.
- [ ] Buyer back-buttons mid-stub-checkout → cart cleared, no order. Either delay cart-clear until payment captured, OR restore cart on `Order.status = CANCELLED`.

## P3 — UI/UX Polish (from Section 11)

- [ ] Landing page: hero, social proof, shop spotlight
- [ ] Empty states for every list view (cart, wishlist, products list, search)
- [ ] Switch plain `<img>` to `next/image` with `srcset` (requires CDN config)
- [ ] Consolidate per-module components into shared shadcn primitives
- [ ] Saved addresses + saved payment methods on `/buyer/account`
- [ ] "You may also like" on `/products/[slug]` (compounds with D2)
- [ ] Run `/plan-design-review` before implementing expansions

## P3 — Deferred Cathedral Expansions

- [ ] **D5 — Live Shopping rooms** — revisit at v2 after 50+ active shops. Evaluate LiveKit, Stream, Whereby.
- [ ] **D7 — Logistics Choice (multi-carrier)** — add when single-carrier failure rate exceeds 5% or shipping volume crosses 1k/month.
- [ ] **D9 — Wholesale B2B mode** — revisit after 20+ shops have retail traction AND ≥1 US boutique has asked unprompted.

## P3 — Additional Excellence Ideas (not formally proposed, from review notes)

These were considered during the review but didn't make the top-8 cut. Surface them later if relevant.

- [ ] Carbon-aware shipping routing + offset toggle at checkout
- [ ] Curated editorial drops ("Diwali Edit", "Wedding Edit")
- [ ] Seller education platform (in-app 5-min courses on packaging, photography, customs)
- [ ] Subscription boxes ($49-99/mo curated artisan goods)
- [ ] AI buyer assistant ("what would pair with this saree?")
- [ ] Returns workflow with pre-printed labels, photo-evidence-based refunds for damaged
- [ ] Bulk product upload for sellers
- [ ] Referral / loyalty program
- [ ] Buyer→seller pre-purchase messaging
- [ ] Verified-purchase badge on reviews

## Operational

- [ ] Git init + first commit + remote (project is currently pre-VCS)
- [ ] Docker Engine native install (P12 from session — `/tmp/install-docker.sh` written, awaiting `sudo bash` invocation)
- [ ] Docker compose smoke test (`docker compose up -d && npm run db:push && npm run db:seed && npm run build`)
- [ ] CI workflow `.github/workflows/ci.yml` (typecheck + lint + test + build on PR)
